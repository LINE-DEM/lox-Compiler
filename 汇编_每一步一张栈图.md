# 汇编图解：每一步一张栈图

> 示例代码：

```asm
addx proc
    push ebp
    mov  ebp, esp
    mov  eax, [ebp+8]
    add  eax, [ebp+0Ch]
    mov  esp, ebp
    pop  ebp
    ret
addx endp

main proc
    push 3
    push 4
    call addx
    add  esp, 8
    ret
main endp
```

---

## 读图约定

- 这是 **32 位 x86** 的示意图。
- 栈 **向低地址增长**。
- 图里采用：
  - 上面是高地址
  - 下面是低地址
- `ESP` 指向当前栈顶。
- `EBP` 在函数内部常作为当前栈帧的固定参考点。
- 返回值放在 `EAX`。

为了方便说明，假设初始时：

```text
ESP = 0012FF70
EBP = 0012FFC0
EAX = ?
```

---

## Step 0：调用前

当前还没有为这次 `addx(4, 3)` 调用压参数。

当前寄存器：

```text
ESP = 0012FF70
EBP = 0012FFC0
EAX = ?
```

当前栈：

```text
高地址

    这次调用相关内容还没入栈

低地址
```

---

## Step 1：`push 3`

执行指令：

```asm
push 3
```

含义：

```text
ESP = ESP - 4
[ESP] = 3
```

当前寄存器：

```text
ESP = 0012FF6C
EBP = 0012FFC0
EAX = ?
```

当前栈：

```text
高地址
┌────────────┬──────────────┐
│ 地址       │ 内容         │
├────────────┼──────────────┤
│ 0012FF6C   │ 3            │ <- ESP
└────────────┴──────────────┘
低地址
```

---

## Step 2：`push 4`

执行指令：

```asm
push 4
```

含义：

```text
ESP = ESP - 4
[ESP] = 4
```

当前寄存器：

```text
ESP = 0012FF68
EBP = 0012FFC0
EAX = ?
```

当前栈：

```text
高地址
┌────────────┬──────────────┐
│ 地址       │ 内容         │
├────────────┼──────────────┤
│ 0012FF6C   │ 3            │
├────────────┼──────────────┤
│ 0012FF68   │ 4            │ <- ESP
└────────────┴──────────────┘
低地址
```

说明：

- `4` 是后压入的，所以更靠近栈顶。
- 这也是为什么后面第一个参数会先被取到。

---

## Step 3：`call addx`

执行指令：

```asm
call addx
```

可以近似理解为：

```text
push 返回地址
jmp addx
```

假设返回地址记作 `RET_MAIN`。

当前寄存器：

```text
ESP = 0012FF64
EBP = 0012FFC0
EAX = ?
```

当前栈：

```text
高地址
┌────────────┬──────────────┐
│ 地址       │ 内容         │
├────────────┼──────────────┤
│ 0012FF6C   │ 3            │
├────────────┼──────────────┤
│ 0012FF68   │ 4            │
├────────────┼──────────────┤
│ 0012FF64   │ RET_MAIN     │ <- ESP
└────────────┴──────────────┘
低地址
```

说明：

- 现在栈顶是 **返回地址**。
- 所以参数不是从 `[ebp+4]` 开始，而是更后面。

---

## Step 4：进入 `addx`，执行 `push ebp`

执行指令：

```asm
push ebp
```

含义：

```text
ESP = ESP - 4
[ESP] = 旧 EBP
```

这里旧 `EBP = 0012FFC0`。

当前寄存器：

```text
ESP = 0012FF60
EBP = 0012FFC0
EAX = ?
```

当前栈：

```text
高地址
┌────────────┬──────────────┐
│ 地址       │ 内容         │
├────────────┼──────────────┤
│ 0012FF6C   │ 3            │
├────────────┼──────────────┤
│ 0012FF68   │ 4            │
├────────────┼──────────────┤
│ 0012FF64   │ RET_MAIN     │
├────────────┼──────────────┤
│ 0012FF60   │ 0012FFC0     │ <- ESP
└────────────┴──────────────┘
低地址
```

说明：

- 这里保存的是调用前那一层的 `EBP`。
- 以后退出 `addx` 时要把它恢复回去。

---

## Step 5：`mov ebp, esp`

执行指令：

```asm
mov ebp, esp
```

含义：

```text
EBP = ESP
```

注意：

- 这条 `mov` 不改栈内容
- 它只是把当前栈顶位置“记住”

当前寄存器：

```text
ESP = 0012FF60
EBP = 0012FF60
EAX = ?
```

当前栈：

```text
高地址
┌────────────┬──────────────┐
│ 地址       │ 内容         │
├────────────┼──────────────┤
│ 0012FF6C   │ 3            │
├────────────┼──────────────┤
│ 0012FF68   │ 4            │
├────────────┼──────────────┤
│ 0012FF64   │ RET_MAIN     │
├────────────┼──────────────┤
│ 0012FF60   │ 0012FFC0     │ <- ESP, EBP
└────────────┴──────────────┘
低地址
```

这时就能按 `EBP` 看栈帧：

```text
[ebp]     = 0012FFC0   ; 旧 EBP
[ebp+4]   = RET_MAIN   ; 返回地址
[ebp+8]   = 4          ; 第 1 个参数
[ebp+0Ch] = 3          ; 第 2 个参数
```

---

## Step 6：`mov eax, [ebp+8]`

执行指令：

```asm
mov eax, [ebp+8]
```

含义：

```text
EAX = 内存[EBP+8]
```

先算地址：

```text
EBP + 8 = 0012FF68
```

再取值：

```text
[0012FF68] = 4
```

所以：

```text
EAX = 4
```

当前寄存器：

```text
ESP = 0012FF60
EBP = 0012FF60
EAX = 4
```

当前栈：

```text
高地址
┌────────────┬──────────────┐
│ 地址       │ 内容         │
├────────────┼──────────────┤
│ 0012FF6C   │ 3            │
├────────────┼──────────────┤
│ 0012FF68   │ 4            │ <- [EBP+8]
├────────────┼──────────────┤
│ 0012FF64   │ RET_MAIN     │
├────────────┼──────────────┤
│ 0012FF60   │ 0012FFC0     │ <- ESP, EBP
└────────────┴──────────────┘
低地址
```

说明：

- 栈没有变。
- 只有 `EAX` 变成了 `4`。

---

## Step 7：`add eax, [ebp+0Ch]`

执行指令：

```asm
add eax, [ebp+0Ch]
```

含义：

```text
EAX = EAX + 内存[EBP+0Ch]
```

先算地址：

```text
EBP + 0Ch = 0012FF6C
```

再取值：

```text
[0012FF6C] = 3
```

所以：

```text
EAX = 4 + 3 = 7
```

当前寄存器：

```text
ESP = 0012FF60
EBP = 0012FF60
EAX = 7
```

当前栈：

```text
高地址
┌────────────┬──────────────┐
│ 地址       │ 内容         │
├────────────┼──────────────┤
│ 0012FF6C   │ 3            │ <- [EBP+0Ch]
├────────────┼──────────────┤
│ 0012FF68   │ 4            │ <- [EBP+8]
├────────────┼──────────────┤
│ 0012FF64   │ RET_MAIN     │
├────────────┼──────────────┤
│ 0012FF60   │ 0012FFC0     │ <- ESP, EBP
└────────────┴──────────────┘
低地址
```

说明：

- 现在返回值已经在 `EAX` 里。
- 但函数还没真正返回。

---

## Step 8：`mov esp, ebp`

执行指令：

```asm
mov esp, ebp
```

含义：

```text
ESP = EBP
```

当前寄存器：

```text
ESP = 0012FF60
EBP = 0012FF60
EAX = 7
```

当前栈：

```text
高地址
┌────────────┬──────────────┐
│ 地址       │ 内容         │
├────────────┼──────────────┤
│ 0012FF6C   │ 3            │
├────────────┼──────────────┤
│ 0012FF68   │ 4            │
├────────────┼──────────────┤
│ 0012FF64   │ RET_MAIN     │
├────────────┼──────────────┤
│ 0012FF60   │ 0012FFC0     │ <- ESP, EBP
└────────────┴──────────────┘
低地址
```

说明：

- 这个简单例子里，它看起来像“没变化”。
- 但它的作用是把 `ESP` 恢复到当前栈帧起点。
- 如果函数中间申请过局部变量，这一步就非常关键。

---

## Step 9：`pop ebp`

执行指令：

```asm
pop ebp
```

含义：

```text
EBP = [ESP]
ESP = ESP + 4
```

当前：

```text
[0012FF60] = 0012FFC0
```

所以执行后：

```text
EBP = 0012FFC0
ESP = 0012FF64
```

当前寄存器：

```text
ESP = 0012FF64
EBP = 0012FFC0
EAX = 7
```

当前栈：

```text
高地址
┌────────────┬──────────────┐
│ 地址       │ 内容         │
├────────────┼──────────────┤
│ 0012FF6C   │ 3            │
├────────────┼──────────────┤
│ 0012FF68   │ 4            │
├────────────┼──────────────┤
│ 0012FF64   │ RET_MAIN     │ <- ESP
└────────────┴──────────────┘
低地址
```

说明：

- 当前函数自己的栈帧已经拆掉了。
- `EBP` 也恢复成调用前那一层的值了。

---

## Step 10：`ret`

执行指令：

```asm
ret
```

含义：

```text
EIP = [ESP]
ESP = ESP + 4
```

当前：

```text
[0012FF64] = RET_MAIN
```

执行后控制流回到 `main`。

当前寄存器：

```text
ESP = 0012FF68
EBP = 0012FFC0
EAX = 7
```

当前栈：

```text
高地址
┌────────────┬──────────────┐
│ 地址       │ 内容         │
├────────────┼──────────────┤
│ 0012FF6C   │ 3            │
├────────────┼──────────────┤
│ 0012FF68   │ 4            │ <- ESP
└────────────┴──────────────┘
低地址
```

说明：

- 这时已经回到 `main`。
- 但参数 `4` 和 `3` 还在栈里。

---

## Step 11：`add esp, 8`

执行指令：

```asm
add esp, 8
```

含义：

```text
ESP = ESP + 8
```

执行后：

当前寄存器：

```text
ESP = 0012FF70
EBP = 0012FFC0
EAX = 7
```

当前栈：

```text
高地址

    这次调用压入的参数已经被逻辑上清掉

低地址
```

说明：

- 栈恢复到调用前的状态。
- `EAX = 7` 依然保留着返回值。

---

## 一眼看懂版：关键栈帧结构

只要执行完：

```asm
push ebp
mov  ebp, esp
```

你就应该立刻想到：

```text
高地址

[ebp+0Ch]   第 2 个参数
[ebp+8 ]   第 1 个参数
[ebp+4 ]   返回地址
[ebp   ]   旧 EBP

低地址
```

---

## 如果换成 `ret 8`，最后两步会怎样

如果函数结尾是：

```asm
ret 8
```

那么它会：

1. 先弹出返回地址
2. 再额外清掉 8 字节参数

也就是说，Step 9 之后如果直接执行 `ret 8`，会变成：

当前寄存器：

```text
ESP = 0012FF70
EBP = 0012FFC0
EAX = 7
```

当前栈：

```text
高地址

    返回地址和两个参数都已经一起清掉

低地址
```

所以这时 `main` 就 **不需要再写**：

```asm
add esp, 8
```

---

## 最后只记 5 句话

```text
1. push 会让 ESP 减 4。
2. pop 会让 ESP 加 4。
3. call 会压返回地址。
4. push ebp / mov ebp, esp 会建立栈帧。
5. [ebp+8] 开始通常才是参数。
```

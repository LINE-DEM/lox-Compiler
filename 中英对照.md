# Lox 编译器——中英对照完全指南

---

## 一、语法规则符号说明

在描述编程语言的语法时，我们用一套"元符号"来写规则，就像数学公式里的 +、=、() 一样。

| 符号 | 含义 | 生活类比 |
|------|------|----------|
| `=`  | 定义为 | "汉堡 = 面包 + 肉饼 + 蔬菜" 里的等号 |
| `\|` | 或者，二选一（或多选一） | 菜单上"可选：可乐 \| 果汁 \| 水" |
| `( ... )*` | 括号内的内容，重复 **0次或多次** | "吃零食*" = 可以不吃，也可以一直吃 |
| `( ... )?` | 括号内的内容，出现 **0次或1次（可选）** | "加辣?" = 可以不加，也可以加一次 |
| `" "` 引号内 | 字面符号，代码里原样出现 | `"+"` 就是真实的加号字符 |
| 无引号的词 | 引用另一条规则（相当于函数调用） | `加减运算` 指向下面定义的那条规则 |

---

## 二、语句规则表（Stmt）——程序的骨架

> **通俗理解**：语句是"一件完整的事"，就像一句完整的命令。
> 每条语句用 `;` 结尾，每条顶层语句是 AST 里的一棵树的根。

```
程序         = 语句* EOF
               ↑ 一个程序由若干条语句组成，最后是文件结束

语句         = 函数声明
             | 变量声明
             | if语句
             | while语句
             | for语句
             | print语句
             | return语句
             | 块语句
             | 表达式语句
               ↑ 语句有这几种，解析时逐一判断是哪种

函数声明      = "fun" 函数名 "(" 参数列表 ")" "{" 语句* "}"
               例：fun add(a, b) { return a + b; }

变量声明      = "var" 变量名 ( "=" 表达式 )? ";"
               例：var x = 10;   或   var x;

if语句        = "if" "(" 表达式 ")" 语句 ( "else" 语句 )?
               例：if (x > 0) print x;   或   if (x > 0) print x; else print 0;

while语句     = "while" "(" 表达式 ")" 语句
               例：while (i < 10) { i = i + 1; }

for语句       = "for" "(" (变量声明 | 表达式语句 | ";") 表达式? ";" 表达式? ")" 语句
               例：for (var i = 0; i < 10; i = i + 1) print i;

print语句     = "print" 表达式 ";"
               例：print 1 + 2;

return语句    = "return" 表达式? ";"
               例：return a + b;   或   return;

块语句        = "{" 语句* "}"
               例：{ var x = 1; print x; }   ← 花括号包起来的一组语句

表达式语句    = 表达式 ";"
               例：x = 5;   或   add(1, 2);  ← 没有 print/if 等关键字，直接是表达式
```

---

## 三、表达式规则表（Expr）——程序的血肉

> **通俗理解**：表达式是"能算出一个值的东西"，比如 `1+2`、`a > 0`、`add(3,5)`。
> 表达式规则从上到下**优先级越来越高**，越靠下越先算。

```
表达式        = 赋值
               ↑ 表达式的入口，最低优先级

赋值          = 或运算
             | 变量名 "=" 赋值
               例：x = 5   或   x = y = 10（右结合）

或运算        = 与运算 ( "or" 与运算 )*
               例：a or b or c

与运算        = 相等判断 ( "and" 相等判断 )*
               例：a and b and c

相等判断      = 大小比较 ( ("!=" | "==") 大小比较 )*
               例：a == b   或   a != b

大小比较      = 加减运算 ( (">" | ">=" | "<" | "<=") 加减运算 )*
               例：a > b   或   a <= b

加减运算      = 乘除运算 ( ("+" | "-") 乘除运算 )*
               例：1 + 2 - 3

乘除运算      = 一元运算 ( ("*" | "/") 一元运算 )*
               例：2 * 3 / 4

一元运算      = ("-" | "!") 一元运算
             | 函数调用
               例：-5   或   !true   或   --x（双重否定，递归）

函数调用      = 基本量 ( "(" 参数列表 ")" )*
               例：add(3, 5)   或   getFunc()(1, 2)（返回函数再调用）

基本量        = 数字
             | 字符串
             | true | false | nil
             | 变量名（IDENTIFIER）
             | "(" 表达式 ")"
               例：42   或   "hello"   或   x   或   (1 + 2)
```

---

## 四、优先级一览（从低到高）

> **通俗理解**：优先级越高 = 越先被计算 = 在树里越靠近叶子节点。

| 层级 | 规则 | 运算符 | 记忆口诀 |
|------|------|--------|----------|
| 最低 | 赋值 | `=` | 最后才把值存进去 |
| ↓ | 或运算 | `or` | 逻辑或，短路 |
| ↓ | 与运算 | `and` | 逻辑与，短路 |
| ↓ | 相等判断 | `== !=` | 判断相不相等 |
| ↓ | 大小比较 | `> >= < <=` | 比较大小 |
| ↓ | 加减 | `+ -` | 加减法 |
| ↓ | 乘除 | `* /` | 乘除法 |
| ↓ | 一元 | `- !` | 取负/取反 |
| ↓ | 函数调用 | `()` | 调用函数 |
| 最高 | 基本量 | 数字/字符串/变量 | 不可再拆的原子 |

---

## 五、用 `1 + 2 * 3` 逐步走一遍规则

**目标**：理解为什么 `*` 比 `+` 先算。

### 第一步：调用链下降

```
expression()          ← 入口
  └─ assignment()
       └─ or()
            └─ and()
                 └─ equality()
                      └─ comparison()
                           └─ term()          ← 加减运算，在这里处理 "+"
```

### 第二步：term() 处理加号

```
term() {
    left = factor()   ← 先去拿左边的值，factor 会处理 * /

    看到 "+"，进入循环：

    right = factor()  ← 再去拿右边的值

    return Binary(left, "+", right)
}
```

### 第三步：factor() 内部处理 `2 * 3`

```
factor() {
    left = unary()   → 得到 2

    看到 "*"，进入循环：

    right = unary()  → 得到 3

    return Binary(2, "*", 3)   ← 先把 2*3 打包好再返回
}
```

### 第四步：最终树的形状

```
term() 拿到了：
  left  = Literal(1)          ← 第一次 factor() 返回的
  right = Binary(2, *, 3)     ← 第二次 factor() 返回的（已经打包好）

最终：Binary(1, +, Binary(2, *, 3))
```

画成树：

```
        +
       / \
      1   *
         / \
        2   3
```

**结论**：`*` 在树的更深处（更靠近叶子），所以执行时从叶子向上算，`2*3` 先算，结果再和 `1` 相加。
**优先级高 = 规则层更深 = 函数调用更深 = 在树里更靠下 = 先算。**

### 第五步：解释器执行验证

解释器遍历 AST 时，采用**后序遍历**（先算子节点，再算父节点）：

```
步骤1：计算左子树  → 1
步骤2：计算右子树
         步骤2a：计算 2
         步骤2b：计算 3
         步骤2c：2 * 3 = 6
步骤3：1 + 6 = 7   ✓
```

---

## 六、两张规则表的衔接关系

```
语句规则（Stmt）是外壳
        │
        ├── print语句 ──────────────────┐
        │                              │
        ├── if语句 ── condition ────────┤
        │                              ↓
        ├── while语句 ── condition ── expression()  ← 跳入表达式规则（Expr）
        │                              ↑
        ├── return语句 ── value ────────┤
        │                              │
        └── 表达式语句 ────────────────┘
```

**语句负责结构**（这件事怎么做），
**表达式负责计算**（值是什么）。
每当语句需要一个"值"时，就调用 `expression()` 跳入表达式规则。

---

## 七、AST 核心概念

**AST = Abstract Syntax Tree = 抽象语法树**

| 概念 | 解释 | 例子 |
|------|------|------|
| **节点（Node）** | 树里的每一个方块，代表一个语法结构 | `Binary(+)`、`Literal(1)` |
| **根节点（Root）** | 树的最顶部，一棵树只有一个根 | `Stmt.Print` |
| **叶子节点（Leaf）** | 没有子节点的节点 | `Literal(3)`、`Variable("x")` |
| **子节点（Child）** | 某个节点下面的节点 | `Binary` 的左右两侧 |
| **森林（Forest）** | 多棵树放在一个列表里 | `List<Stmt>` |
| **一棵树** | 一条顶层语句 = 一棵树 | `print 1+2;` |
| **多棵树** | 多条顶层语句 = 森林 | `print 1; print 2;` → 2棵树 |

**一句话总结**：
Parser 把一串 Token（词）按照语法规则，组装成一棵棵树，
Interpreter 再从根往下走这棵树，执行每个节点的逻辑，得出结果。

---

## 八、对应到 Parser.java 的函数位置

| 规则名 | 对应函数 |
|--------|----------|
| 程序入口 | `parse()` |
| 语句分发 | `statement()` |
| 函数声明 | `function()` |
| 变量声明 | `varDeclaration()` |
| if语句 | `ifStatement()` |
| while语句 | `whileStatement()` |
| for语句 | `forStatement()` |
| print语句 | `printStatement()` |
| return语句 | `returnStatement()` |
| 块语句 | `block()` |
| 表达式入口 | `expression()` |
| 赋值 | `assignment()` |
| 或运算 | `or()` |
| 与运算 | `and()` |
| 相等判断 | `equality()` |
| 大小比较 | `comparison()` |
| 加减运算 | `term()` |
| 乘除运算 | `factor()` |
| 一元运算 | `unary()` |
| 函数调用 | `call()` / `finishCall()` |
| 基本量 | `primary()` |
